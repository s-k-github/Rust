Traits allow us to define a set of methods that are shared across different types.
trait as parameter:
    here we pass trait as parameter to a function
    we can use trait's arguments in that function
trait bounds:
    In Rust, trait bounds are constraints that you place on generic types to specify what capabilities (i.e., traits) those types must implement. They allow you to use generics with functionality like methods or operations that require specific traits.

