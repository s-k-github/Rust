1. print: println multiple things on same line
2. eprintln:println standard error stream instead of standard output stream. useful when printing error msgs
3. format:creates new string with given format. useful when want to create string that has multiple values or use more advanced formatting option.
4. write:similar to format but writes to a buffer rather than returning a new string.

! denotes its a macro not a function that is used from Rust standard library.

